q-1
ALTER TABLE EMP
ADD REPORTS_TO constraint FK_REPORTS_TO_EMPCODE references EMPCODE;

Q-2
ALTER TABLE SKILL
ADD constraint SKILL_check CHECK(SKILLID LIKE '%S');
 
Q-3
ALTER TABLE EMP_SKILL
ADD constraint PK_EMPNO_SKILLID PRIMARY KEY (EMPNO,SKILLID);

Q-4
ALTER TABLE PRJ_DETAILS
ADD constraint UNQ_PRJ_NAME UNIQUE (PRJ_NAME);

Q-5
ALTER TABLE CLIENT
ADD constraint VALID_EMAIL CHECK(EMAIL LIKE'%@%.3_');

Q-6
ALTER TABLE EMP
ADD constraint UNQ_PHONE UNIQUE(PHONE);

Q-7
ALTER TABLE EMP
ADD constraint UNQ_EMAIL UNIQUE(EMAIL);


q-8
ALTER TABLE MEDICAL_POLICY
DROP constraint constraint_name
ALTER TABLE MADICAL_POLICY
ADD constraint constraint_name PRIMARY KEY(POLICYNO,POLICYDATE);

Q-9
ALTER TABLE MEDICAL_POLICY
DROP constraint constraint_name
ALTER TABLE MEDICAL_POLICY
ADD constraint newconstraint_name CHECK(PREMIUM>1000);
